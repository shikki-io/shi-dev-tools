.build/
*.xcodeproj/
*.xcworkspace/
DerivedData/
.swiftpm/
*.o
*.d

# Shikki kernel placeholder — auto-created by shi launch, not a dependency
/projects/shikki/
# Secrets — env files and every backup of them never belong in git.
# A backup is git history, not a copy next to the file.
.env
.env.*
*.env
*.env.bak*
*.env.back*
!.env.example
!.env.template
!.env.sample
!.env.dist
!*.env.example
!*.env.template
